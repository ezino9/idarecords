<h2 class="page-header"><%= @category.name%></h2>
<div class="row">
   <div class="col-md-10">
      <div class="row">
        <% if @posts.exists? %>
            <% @posts.each do |post| %>
               <div class="col-md-4">
                   <div class="panel panel-default">
                      <div class="panel panel-heading">
                         <%= link_to  post.title, post_path(post) %> 
                      </div>
                      <div class="panel panel-body" id="post-index">
                          <% if post.coverpicture.present? %>
                            <%= image_tag  "#{post.coverpicture}", width:'100%' %>
                          <% else %>
                            <%= image_tag  "img/image2.jpg", width:'100%' %>
                          <% end %><br/>
                          <%= raw post.body.truncate(100)%>
                          <p><strong>Posted on: </strong><%= post.created_at.to_time.strftime('%b %e, %Y')%></p>
                          <p><strong>Posted by: </strong><%= post.user.name%></p>
                      </div>
                      <div class="panel panel-footer">
                          <%= link_to  "Read more", post_path(post), class:'btn btn-block btn-success' %>
                      </div> 
                   </div>
               </div>
            <% end%>
        <!-- pagination -->
        <% else %>
           <p>No post available!!</p>
        <% end %> 
      </div> 
   </div>
   <div class="col-md-2">
   </div> 
</div>